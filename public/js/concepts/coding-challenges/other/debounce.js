// func = function()
// wait = int (Millisec)
// immediate = boolean (t/f) => if T then
function debounce(func, wait, immediate) {
  func.prototype.
  var val = setTimeout(called, wait);

  function called() {
    return func;
  }
  return val;
}
